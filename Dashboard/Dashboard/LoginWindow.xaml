<Window x:Class="Dashboard.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dashboard"
        mc:Ignorable="d"
        Title="Login" Height="350" Width="400"
        Background="#2d1b4e">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label FontSize="35" FontWeight="DemiBold" HorizontalAlignment="Center" Margin="25" Foreground="#fff">User management</Label>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                <Label FontSize="25" Margin="10" Foreground="#fff">Email:</Label>
                <TextBox x:Name="EmailTextBox" Height="35" Width="250" Background="#1a0f2e" BorderBrush="#463956" Foreground="White" Padding="5">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                <Label FontSize="25" Margin="10" Foreground="#fff">Password:</Label>
                <PasswordBox x:Name="PasswordBox" Height="35" Width="200" Background="#1a0f2e" BorderBrush="#463956">
                    <PasswordBox.Template>
                        <ControlTemplate TargetType="PasswordBox">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="6"
                                    Padding="5">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </PasswordBox.Template>
                </PasswordBox>
            </StackPanel>

            <Button x:Name="LoginButton" Height="35" Width="150" FontSize="20" FontWeight="DemiBold" Margin="20"
                Background="#7c3aed" BorderBrush="#463956" Foreground="#fff" Click="LoginButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                Login
            </Button>
        </StackPanel>
    </Grid>
</Window>
