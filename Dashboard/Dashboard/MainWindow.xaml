<Window x:Class="Dashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dashboard"
        mc:Ignorable="d"
        Title="Dashboard" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label FontSize="15" Margin="10">Search for users:</Label>
            <TextBox Height="25" Width="200"/>
            <StackPanel Width="315"/>
            <Button Height="25" Width="100" Margin="10">Logout</Button>
        </StackPanel>
        <GroupBox Margin="5" Padding="5" Header="Users" Grid.Row="1">
            <DataGrid Grid.Row="1" x:Name="dataGrid" HeadersVisibility="Column" AutoGenerateColumns="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Width="*" Header="Fisrt Name" Binding="{Binding FirstName}"/>
                    <DataGridTextColumn Width="*" Header="Last Name" Binding="{Binding LastName}"/>
                    <DataGridTextColumn Width="*" Header="Email" Binding="{Binding Email}"/>
                    <DataGridTextColumn Width="*" Header="Role" Binding="{Binding Role}"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
